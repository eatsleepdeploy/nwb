---
import type {Image} from "@lib/types";

interface Props {
    image?: Image;
    class?: string
    style?: object
}
const {class: kls = '', image, style = {}} = Astro.props;
---
{image && <img
    class={kls}
    src={image.src}
    alt={image.alt}
    style={{...style}}
/>}
